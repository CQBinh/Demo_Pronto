dependencies:
  post:
    - chmod +x bin/cisetup
    - bin/cisetup
    - bundle exec pronto run -f github_pr --exit-code
    - bundle exec rake foo:var

checkout:
  post:
    - git fetch origin --depth=1000000
